{
  "name": "midnight-authenticator",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-knowledge TOTP authenticator for Midnight blockchain",
  "author": "subc0der",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:contracts": "pnpm --filter @midnight-authenticator/contracts compile && pnpm --filter @midnight-authenticator/contracts build",
    "deploy": "pnpm --filter @midnight-authenticator/deploy-cli deploy",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "overrides": {
      "smoldot": "npm:empty-npm-package@1.0.0",
      "@midnight-ntwrk/ledger-v7": "7.0.2",
      "@midnight-ntwrk/compact-js": "2.4.3"
    }
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
